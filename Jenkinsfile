pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Etapa de Build"
            }
         stage('scan') {
            steps {
                echo "Etapa Scan"
        }
         stage('deploy') {
            steps {
                echo "Etapa Deploy"
        }
    }
}
